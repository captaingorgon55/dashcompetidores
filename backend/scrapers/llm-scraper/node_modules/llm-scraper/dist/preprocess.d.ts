import { type Page } from 'playwright';
export type PreProcessOptions = {
    format?: 'html' | 'text' | 'markdown' | 'raw_html';
} | {
    format: 'custom';
    formatFunction: (page: Page) => Promise<string> | string;
} | {
    format: 'image';
    fullPage?: boolean;
};
export type PreProcessResult = {
    url: string;
    content: string;
    format: PreProcessOptions['format'];
};
export declare function preprocess(page: Page, options?: PreProcessOptions): Promise<PreProcessResult>;
